<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.exercise2">

  <application
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/Theme.Exercise2">
    <activity android:name=".KodeSewaTampil"></activity>
    <activity android:name=".MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity
        android:name=".DetailBook"
        android:parentActivityName=".DaftarB" />
    <activity
        android:name=".DaftarB"
        android:parentActivityName=".MainActivity" />
    <activity
        android:name=".SewaBukuAct"
        android:parentActivityName=".MainActivity" />
    <activity
        android:name=".TambahBuku"
        android:parentActivityName=".DaftarB" />
  </application>

</manifest>